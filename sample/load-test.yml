config:
  target: 'http://localhost:8000'
  phases:
    - duration: 60
      arrivalRate: 20
scenarios:
  - name: "Custom engine scenario"
    engine: "grpc"
  - flow:
    - get:
        url: "/docs"
